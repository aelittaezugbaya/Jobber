/*

import React from 'react';
import {shallow} from 'enzyme';
import Checkbox from '../Checkbox';
import Adapter from 'enzyme-adapter-react-15';
import Enzyme from 'enzyme';

//import { configure } from 'enzyme';
//import * as ReactSixteenAdapter from 'enzyme-adapter-react-15';
//configure({ adapter: new ReactSixteenAdapter() });

Enzyme.configure({ adapter: new Adapter() });

test('Checkbox changes the text after click', () => {
  // Render a checkbox with label in the document
  const wrapper = shallow(
    <div>
      <Checkbox id="beauty" text="Beauty"/>
    </div>
  );

  expect(wrapper.contains('hannu is an ass')).toEqual(false);
  //expect(wrapper.contains(<Checkbox id="beauty" />)).to.equal(true);


});

*/
